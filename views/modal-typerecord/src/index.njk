<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self'">
    <meta http-equiv="X-Content-Security-Policy" content="default-src 'self'; script-src 'self'">
    
    <link rel="stylesheet" href="../../style.css">
</head>

<body>
    
    <main>

        <form>
            <label>
                {{ lang.fields.name[flag] }}
                <input type="text" name="name">
            </label>

            <label>
                {{ lang.fields.color_fill[flag] }}
                <input type="color" name="fill">
            </label>

            <label>
                {{ lang.fields.color_stroke[flag] }}
                <input type="color" name="stroke">
            </label>

            <input type="hidden" name="initial_name">

            <input type="hidden" name="action">

            <div class="cancel-ok">
                <button class="window-close" type="button">{{ langBtnCancel[flag] }}</button>
                <button class="primary" type="submit">{{ langBtnOk[flag] }}</button>
            </div>
        </form>

    </main>

    <script src="../render.js"></script>
</body>

</html>